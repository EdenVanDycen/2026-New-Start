let number = 5;
const leftBorderWidth = 1;

number = 10;
console.log(number);

const obj = {
  a: 50
};
obj.a = 10
console.log(obj);

//Request
// data
// response
//Серверные части(запросы)

// snake_case
//UPPERS_SNAKE_CASE Называют константы которые нельзя переопределять 
//Kebab-case
//PascalCase 

let COLOR_RED = '#F00'; // Константы которые нельзя менять без соглосования с командой 

const _apiBase = 'https://gateway.marvel.com:443/v1/public/'; // Такие переменные нельзя трогать без согласования с командой 
const _apiKey = 'apikey=d4eecb0c66dedbfae4aeb45d312fc1df'; // Такие переменные нельзя трогать без согласования с командой 
